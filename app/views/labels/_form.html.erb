<%= form_with(model: @label, local: true) do |f| %>
  <div class="form-group mt-4">
    <%= f.label :labelname %>
    <%= f.text_area :labelname , class: "form-control" %>
  </div>
  <%= f.submit "登録", class: "btn btn-primary mt-4" %>
<% end %>
